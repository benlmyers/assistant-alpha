def get_parameters(endpoint, method, spec_source, spec_data):

    parameters = []

    # TODO
    # Return various parameters.
    # Path parameters: Substituted into the endpoint path (e.g. /pets/{id})
    # Query parameters: Used at the end of a URL (e.g. /pets?id=123)
    # Header parameters: Information passed in request header
    return parameters
